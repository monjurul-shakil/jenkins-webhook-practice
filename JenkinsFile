pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Build triggered by GitHub webhook"
                echo "Branch: ${env.BRANCH_NAME}"
            }
        }
    }
}

